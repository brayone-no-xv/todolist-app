if statement:
const enter = document.getElementById("enter");
if (input.value != li.length === "4") 
if (input.value != " ") {
      input.Value = "Valid";
    } else {
      window.alert("Invalid");
    }

for:

for(let enter = document.getElementById("enter"); enter < 2; enter++) {
  enter.onclick = function() {
  ul.appendChild(li);
  li.appendChild(document.createTextNode(input.nodeValue));
  input.Value = " ";
}